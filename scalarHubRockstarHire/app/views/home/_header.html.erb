<nav class="navbar navbar-expand-lg bg-info-subtle" style="position: sticky; top: 0; background-color: info-subtle; z-index: 100;">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link active" %>
        </li>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "New image", new_image_link_path, class: "nav-link active" %>
          </li>
        </ul>
      </ul>

      <%= form_tag(search_image_links_path, method: :get, class: "d-flex") do %>
        <input class="form-control me-2" type="search" name="tag" placeholder="Search" aria-label="Search" value="<%= params[:tag] %>">
        <button class="btn btn-outline-success" type="submit">Search</button>
      <% end %>
    </div>
  </div>
</nav>
